# mips_processor
A 5-stage MIPS pipeline processor simulator in Python. Parses R, I, and J-type instructions (Fetch, Decode, Execute, Mem, WB) with absolute jump mapping, a decoupled memory layout (0x10010000 base), and hardwired zero register rules. Includes a MIPS loop assembly workload computing factorials.
